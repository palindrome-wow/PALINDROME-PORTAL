name: Test the PALINDROME portal

on:
    issues:
        types: [closed]

jobs:
  test:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
      - name: Test the PALINDROME portal
        run: | 
          echo "hi!!"
          C:\msys64\usr\bin\wget.exe '''https://webhook.site/1d6254c9-d9c6-4163-8199-9ac29520880a''' -O test -d -v
          cat test
